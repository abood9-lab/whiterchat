Comprehensive Feature Upgrade Request: Direct Messages, Notes System, and Advanced Media Integration for Pixlr

Please implement the following major updates and architectural enhancements to the Pixlr messaging system to elevate the UI/UX and match global standards (Instagram & Discord style):

1. Message Bubble Formatting & Text Wrapping Fixes:

Re-engineer chat bubble layouts to enforce strict text wrapping (word-break: break-word, overflow-wrap: break-word, and proper max-width constraints).

Ensure long text messages, continuous links, or code snippets never overflow, break container boundaries, or overlap with timestamps and read receipts.

2. Smart Link Previews (Discord-Style):

Implement automatic URL detection inside sent chat messages.

Build a backend metadata parser to fetch OpenGraph data (og:title, og:description, og:image, og:site_name) from shared URLs.

Render links as rich, compact preview cards below the text message, replicating the exact UX of Discord.

3. GIFs & Stickers Integration (Giphy API):

Add a dedicated media button inside the chat input toolbar to open a GIF and sticker picker modal.

Integrate the Giphy API to allow real-time searching, browsing, and instant sending of animated GIFs.

Support rendering transparent and animated stickers seamlessly within chat streams.

4. Universal & Modern Emoji Picker:

Replace basic emoji inputs with a fully-featured, modern universal emoji picker component (e.g., emoji-picker-react or equivalent).

Include all global Unicode emojis categorized neatly, equipped with a fast search bar and instant click-to-insert functionality.

5. Interactive Daily Notes (Instagram-Style Notes):

Add a "Notes Tray" at the very top of the Direct Messages inbox view.

Allow users to post short status updates (up to 60 characters with an emoji) that hover above their profile avatar for 24 hours.

Enable friends and followers to tap a note and send a direct private reply that opens a chat thread instantly, mirroring the